# Session Notes

Last updated: 2026-05-26

## Latest Goal Progress (2026-05-26)

- **Sector-aware scanner, Binance Alpha treatment, and sector rotation deployed:** Expanded sector classification so the scanner universe is no longer manually brittle. `config/binance_alpha_assets.yaml` now carries active Binance Alpha assets, `strategy_passport` supports dynamic sector registration from Binance `underlyingType`/`underlyingSubType`, and VM live scanner verification now shows `eligible=56` with `other=[]` (including auto-classified `MU -> tradfi_equity`, `DOT -> l1`, `DRIFT -> defi`). Added conditional Binance Alpha risk adjustment in `Scorer`: risky Alpha setups get soft score and size penalties for `setup_quality < 65`, weak participation (`min(volume_confirmation, open_interest) < 55`), and extreme volatility (`volatility >= 75`), while clean Alpha setups are not penalized. Added `src/models/sector_rotation.py`: computes per-cycle sector return vs BTC, breadth, volume acceleration, OI change, rank, confidence, and state (`rotating_in`, `rotating_out`, `firming`, `weakening`, etc.) from already-fetched snapshots, attaches it to snapshots, applies direction-aware soft score multipliers, and persists sector-rotation fields into setup passport, pending scores, and dataset rows. VM runtime evidence after deploy/restart: active `screen -S ninja-trader-vm`, pid `448557`, heartbeat `equity=$986.54`, scanner found `56` eligible pairs, and log shows `Sector rotation top: l2 rotating_in`; no fresh `Traceback`/`ERROR`/`Unhandled`/exception in checked tails. Local verification: sector rotation/context/market-data/persistence focused suite -> `22 passed`; compileall and `git diff --check` passed. VM verification: compileall for touched runtime files passed; sector-rotation focused tests -> `6 passed`; live scanner sector check -> `other=[]`. During deploy an accidental scp to `src/data/` created local VM artifacts (`src/data/scorer.py`, `src/data/main.py`, `src/data/strategy_passport.py`); they were generated by this session and were removed before VM compile/tests/restart.

## Latest Goal Progress (2026-05-24)

- **Google AI Studio Gemini key wired into public LLM provider path:** User provided a Google AI Studio API key; treated it as secret and did not commit it. Added `gemini` provider to the market hot narrative LLM registry and config, using Google’s official OpenAI-compatible endpoint `https://generativelanguage.googleapis.com/v1beta/openai/chat/completions`, model `gemini-2.5-flash`, env keys `GEMINI_API_KEY`/`GOOGLE_API_KEY`, and made `auto` prefer Gemini before OpenRouter/Groq/Hugging Face/OpenAI when the key is present. Added regression coverage that Gemini is selected first and that non-Gemini fallbacks still work. Stored the provided key only in VM `/home/kiosarwin/ninja_trader/.env` as `GEMINI_API_KEY` with `0600` permissions. Sanitized live API test returned HTTP `200` and content `{"ok":true}`. Local verification: `tests/test_crypto_news.py tests/test_config_schema.py::test_pydantic_model_can_be_built_directly` -> `9 passed`; compileall and `git diff --check` passed. Deployed `src/data/crypto_news.py`, `config/config.yaml`, and `tests/test_crypto_news.py` to VM; VM compileall passed; VM focused tests -> `9 passed`; VM provider check shows `candidate_count=1`, first provider `gemini`, model `gemini-2.5-flash`; restarted `screen -S ninja-trader-vm`, active pid `341985`; post-restart log scan found no fresh `Traceback`/`ERROR`/`Unhandled`/`Telegram error`/crypto-news failures.

- **Public LLM API providers added for market hot narratives:** Extended `src/data/crypto_news.py` from single OpenAI-compatible URL support to a provider registry with `openrouter`, `groq`, `huggingface`, and `openai`, selected by `telegram.crypto_news_llm_provider=auto` and available env keys. Default public route is OpenRouter `https://openrouter.ai/api/v1/chat/completions` with model `openrouter/free`; Groq and Hugging Face use their OpenAI-compatible chat endpoints as fallback providers if `GROQ_API_KEY` or `HF_TOKEN` is present. Existing deterministic narrative fallback remains active when no provider key exists or all providers fail. Config now lists public providers and key env names without storing secrets. Local verification: `tests/test_crypto_news.py tests/test_main_reporting.py::test_maybe_send_crypto_news_report_runs_on_independent_interval tests/test_config_schema.py::test_pydantic_model_can_be_built_directly` -> `9 passed`; notifier/news/config slice -> `38 passed`; compileall for touched files and `git diff --check` passed. Deployed `src/data/crypto_news.py`, `config/config.yaml`, and `tests/test_crypto_news.py` to VM; VM compileall passed; VM focused suite -> `38 passed`; restarted `screen -S ninja-trader-vm`, active pid `341256`; post-restart log scan found no fresh `Traceback`/`ERROR`/`Unhandled`/`Telegram error`/crypto-news failures. VM provider config is active (`provider=auto`, `openrouter_model=openrouter/free`), but current `candidate_count=0` because no provider API key is present in the VM environment, so the rule-based fallback will run until a key such as `OPENROUTER_API_KEY` is added.

- **Telegram crypto brief changed to market hot narrative:** Replaced the old `BTC & ALTCOIN URGENT NEWS — TOP 5` headline list with `MARKET HOT NARRATIVE — TOP 5`. Added `MarketNarrative` clustering in `src/data/crypto_news.py`: deterministic rule-based narrative buckets by default, plus optional OpenAI-compatible LLM clustering when `telegram.crypto_news_llm_enabled=true` and `OPENAI_API_KEY` is available. `NinjaTrader._maybe_send_crypto_news_report()` now fetches RSS headlines, builds narratives, and sends the narrative report. Config defaults keep LLM off and add model/API/timeout settings. Local verification: `tests/test_crypto_news.py tests/test_main_reporting.py::test_maybe_send_crypto_news_report_runs_on_independent_interval` -> `7 passed`; notifier/config slice -> `37 passed`; compileall for touched files passed; `git diff --check` passed. Deployed `src/data/crypto_news.py`, `src/notifications/telegram.py`, `src/main.py`, `config/config.yaml`, `tests/test_crypto_news.py`, and `tests/test_main_reporting.py` to VM; VM compileall passed; VM focused suite -> `37 passed`; VM config schema check -> `1 passed`; restarted `screen -S ninja-trader-vm`, active pid `340453`; post-restart log scan found no `Traceback`/`ERROR`/`Unhandled`/`Telegram error`/crypto-news failures.

- **Time-series + Markov diagnostics layer implemented from RohOnChain articles:** User shared two X articles on time-series/ARIMA-GARCH style frameworks and Markov Chains. Added a lightweight production-safe `src/models/time_series.py` layer instead of heavy runtime ARIMA/GARCH fitting: uses log returns, EWMA volatility, return shock z-score, stability score, and observable 3-state Markov transition probabilities (`bull`/`bear`/`sideways`) from rolling returns. `MarketDataService` attaches diagnostics to each `MarketSnapshot`; `Scorer` applies direction-aware soft score and size multipliers, including Markov bull-vs-bear confirmation/conflict; dataset logger and pending trade scores persist the diagnostics for later attribution. Added config under `time_series:` and tests in `tests/test_time_series.py` plus market-data attachment coverage. Local verification: compile touched files passed; focused tests `tests/test_time_series.py tests/test_market_data_service.py tests/test_market_context.py::test_dataset_logger_extracts_market_context_fields` -> `8 passed`; broader scoring/router slice -> `31 passed`; `git diff --check` passed. Deployed touched files to VM, cleaned accidental root-level scp artifacts, VM compile passed, VM focused suite -> `26 passed`; restarted `screen -S ninja-trader-vm`, active pid `334616`. Fresh runtime scan computed market context, scored candidates through the new scorer path, heartbeat shows `equity=$1000.00`, `open=1`, and checked log tail had no fresh `Traceback`/`ERROR`/`Unhandled`/`Telegram error`.

- **Paper loss-streak guard added from High Probability Trading principles:** User asked to elaborate `055_High probability trading take the steps to become a successful trader(rasabourse.com).pdf` and reduce repeat loss streak risk without restoring halt/cooldown. Extracted the relevant principles as machine rules: avoid overtrading when losing, do not increase size while losing, wait for higher-quality/high-probability setups, keep samples smaller. Added paper-only `loss_streak_guard` in `src/main.py` and `config/config.yaml`: active after daily PnL <= `-2.0%` or `2` consecutive losses, requires score >= active threshold + `8`, setup quality >= `68`, EV present, p_win >= `0.42`, and EV net >= `0.0%`; it does not halt trading or use cooldown, but caps total size to `0.35x` while active and disables the high-conviction paper size floor during the guard. Added regression tests in `tests/test_paper_validation.py`. Local verification: focused guard/stress/threshold tests -> `4 passed`; broader paper/cohort/lifecycle/liquidity slice -> `68 passed`; compileall for touched files and `git diff --check` passed. Deployed `src/main.py`, `config/config.yaml`, and `tests/test_paper_validation.py` to VM; VM focused guard/stress tests -> `3 passed`; VM compileall passed; VM config check prints `$1000` reset token, cooldown `0/0`, max streak halt `0`, guard enabled with streak `2`, p_win `0.42`, size cap `0.35`. Restarted `screen -S ninja-trader-vm`; active pid `328220`; fresh heartbeat shows `equity=$1000.00`, `daily_pnl=0.0%`, `open=0`; checked log tail had no `Traceback`/`ERROR`/`Unhandled`.

- **Rejection telemetry now records router setup cohorts:** Runtime rejection audit after the `$1000` reset showed `data/rejections.parquet` was storing coarse `setup_type` values like `trending_expansion_long`, not the router/passport setup (`trend_continuation`, `liquidity_sweep_reversal`, `no_trade`, etc.). That made bottleneck analysis and cohort postmortems less reliable even though admission behavior was mostly correct. Updated `RejectionLogger` to persist actual `setup_type`, `strategy_sleeve`, and `setup_quality_score`, falling back to the old regime-direction value only when no breakdown/passport setup exists. Local verification: `tests/test_rejection_logger_cycle_summary.py tests/test_paper_validation.py::test_paper_threshold_calibration_collects_mid_30s_trending_samples tests/test_liquidity_sweep_reversal.py` -> `12 passed`; compileall and targeted `git diff --check` passed. Deployed to VM; VM focused rejection telemetry tests -> `7 passed`; VM compileall for rejection logger files passed; restarted `screen` runtime, active pid `326730`; after one fresh scan `data/rejections.parquet` contains new columns `strategy_sleeve` and `setup_quality_score`, and new rows now show actual router setup fields such as `setup_type=trend_continuation`, `strategy_sleeve=trend_following`, `setup_quality_score=67.32` for NIL/ZEC cohort-policy blocks. Heartbeat remains `equity=$1000.00`, `daily_pnl=0.0%`, `open=0`.

- **Lifecycle cohorts now isolate setup-specific edges:** Audit after the `$1000` reset found `StrategyLifecycleManager` still grouped closed trades by generic `(sleeve, regime, sm_phase, session, direction)`, while `CohortPolicy`/attribution already isolate setup-specific cohorts. This could let generic reversal/trend history contaminate `mtf_price_action_continuation`, `vwap_pullback_continuation`, and `liquidity_sweep_reversal`, or make live assessment miss a validated cohort because breakdowns used `session=unknown`. Updated lifecycle keys so setup-specific cohorts use `{setup_type}|{regime}|{direction}` and generic cohorts use the active market session at assessment time. Added regression coverage proving `liquidity_sweep_reversal` remains separate from generic `sweep_reversal` losses and live assessment matches its report key. Local verification: `tests/test_strategy_lifecycle.py tests/test_cohort_policy.py tests/test_strategy_router.py tests/test_paper_validation.py tests/test_main_closed_trade_metadata.py tests/test_learner_state_loading.py tests/test_liquidity_sweep_reversal.py tests/test_mtf_price_action.py tests/test_vwap_pullback_continuation.py` -> `101 passed`; compileall and `git diff --check` passed. Deployed to VM; VM focused lifecycle/cohort/liquidity tests -> `21 passed`; VM compileall for lifecycle files passed; restarted `screen` runtime, active pid `325978`; heartbeat shows `equity=$1000.00`, `daily_pnl=0.0%`, `open=0`; lifecycle report now shows setup-isolated MTF cohorts (`mtf_price_action_continuation|trending_expansion|long/short`) and `primary_edge=None`, which is correct because no cohort is validated yet; fresh boot log error check returned no runtime errors.

- **Paper sample collection reset to $1000 and loss-streak halt disabled by user request:** Updated `trading.paper_starting_equity` to `1000` and bumped `paper_state_reset_token` to `sample_collection_1000_2026_05_25` so the next VM restart resets stale paper state. Reverted streak halt/cooldown for sample collection (`risk.cooldown_after_loss_streak=0`, `risk.cooldown_minutes=0`, `safety.max_consecutive_losses=0`) and kept daily loss cap at `5.0`. Also added an isolated professional-style `liquidity_sweep_reversal` cohort (SMC/ICT liquidity raid + reclaim close) with separate detector/passport/cohort attribution/stress-block wiring, so it does not inherit generic reversal history. Local verification: config load prints `$1000`, reset token, cooldown `0 0`, max streak halt `0`; focused risk/reset/cohort tests -> `14 passed`; broader router/admission/cohort/risk slice -> `83 passed`; compileall and `git diff --check` passed. Deployed to VM; VM config check prints `1000 sample_collection_1000_2026_05_25 0 0 0` and `lsr True 78.0`; VM focused tests -> `14 passed`; VM compileall passed; restarted `screen` runtime, active pid `325748`; startup log confirms stale paper state reset, 1 restored open trade cleared, open trades now `0`, and heartbeat shows `equity=$1000.00`.

- **Passport monitor now covers experimental continuation setups:** Follow-up audit found the restored MTF/VWAP setup passports were not covered by the existing passport monitor because it only recognized generic `trend_continuation` / `impulse_continuation`; open BCH MTF had `setup_type=mtf_price_action_continuation` and `expected_path=mtf_impulse_continuation`. Added experimental continuation monitoring for `mtf_price_action_continuation`, `vwap_pullback_continuation`, `mtf_impulse_continuation`, and `vwap_reclaim_continuation`, with stricter defaults (`experimental_continuation_failure_mae_r=0.50`, `experimental_continuation_mfe_ceiling_r=0.35`) and exit reason `passport_failed_experimental_continuation`. Verification: local focused passport/setup tests -> `13 passed`; local broader exit/admission/router slice -> `80 passed`; local compileall and `git diff --check` passed. Deployed `src/execution/trade_manager.py`, `config/config.yaml`, and `tests/test_trade_manager_exit_profiles.py` to VM; VM focused passport tests -> `3 passed`; VM compileall passed; VM config check prints `0.5 0.35`; restarted `screen` runtime, active pid `322770`; fresh log tail had no `Traceback`/`ERROR`/`Unhandled`/`Telegram error`. After restart EIGEN closed by normal stop loss; open state is now 1 trade (`BCH/USDT` short MTF, MFE ~0.42R / MAE ~0.44R), not immediately closed by the new passport monitor because it already showed meaningful MFE above the configured ceiling.

- **Experimental continuation cohorts tightened under stress:** Audit after restoring MTF/VWAP cohorts showed fresh MTF entries could appear during bad-day drawdown with `thin participation` and `dispersion high`. Tightened `StrategyRouter` so `mtf_price_action_continuation` and `vwap_pullback_continuation` require volume+OI participation and are blocked when cross-sectional dispersion is `high`; valid experimental signals blocked by dispersion no longer fall back into generic `trend_continuation`. Added a paper-validation admission guard that blocks unproven experimental setup types (`mtf_price_action_continuation`, `vwap_pullback_continuation`) when paper stress is active (`daily_pnl <= -3.0%` or loss streak >= 3), while leaving core setup/risk-cap behavior intact. Verification: local focused router/admission tests -> `27 passed`; local broader slice `tests/test_paper_validation.py tests/test_cohort_policy.py tests/test_strategy_router.py tests/test_mtf_price_action.py tests/test_vwap_pullback_continuation.py` -> `80 passed`; local compileall and `git diff --check` passed. Deployed `src/models/strategy_router.py`, `src/main.py`, `config/config.yaml`, and tests to VM; VM focused suite -> `27 passed`; VM compileall passed; VM config check prints experimental stress guard enabled with setup list; restarted `screen` runtime, active pid `321931`; fresh log tail had no `Traceback`/`ERROR`/`Unhandled`/`Telegram error`. Latest VM heartbeat shows bad-day state (`equity=$65.83`, `daily_pnl=-6.0%`, open `2`), so new experimental continuation entries are now stress-blocked.

- **VWAP pullback continuation cohort added:** Added a separate pro-style trend setup `vwap_pullback_continuation`, based on trend-aligned pullback/reclaim around rolling VWAP with volume confirmation. VWAP is an institutional execution benchmark; this cohort is treated as an experimental high-selectivity setup, not assumed profitable before its own forward samples. Added `src/analysis/vwap_pullback.py`, wired `Scorer` and `StrategyRouter`, set passport fields (`setup_type=vwap_pullback_continuation`, `expected_path=vwap_reclaim_continuation`), and isolated cohort attribution from generic `trend_following` history. Config enables it with `min_quality_score=74.0`, `vwap_period=20`, `pullback_lookback=6`, `min_volume_ratio=1.15`, and `max_atr_distance=0.85`. Verification: local `./venv/bin/pytest -q tests/test_vwap_pullback_continuation.py tests/test_mtf_price_action.py tests/test_strategy_router.py` -> `21 passed`; local cohort/paper slice -> `14 passed`; local compileall and `git diff --check` passed. Deployed to VM; VM same focused suite -> `21 passed`; VM compileall passed; VM config load prints `True 74.0`; restarted `screen` runtime, active pid `304035`; fresh log tail had no `Traceback`/`ERROR`/`Unhandled`/`Telegram error`. Post-restart runtime also opened a fresh `FXS/USDT` short via the restored MTF price-action continuation cohort.

- **MTF price-action continuation restored:** Reinstalled `mtf_price_action_continuation` as a dedicated trend-following setup. Added `src/analysis/mtf_price_action.py`, wired `Scorer` to detect HTF trend + pullback hold + primary continuation break, routed valid setups through `StrategyRouter` with setup passport fields (`setup_type=mtf_price_action_continuation`, `expected_path=mtf_impulse_continuation`), and isolated attribution/cohort matching by setup type so generic weak `trend_following` history does not automatically block this restored setup. Backtest reports now show setup breakdown. Verification: local `./venv/bin/pytest -q tests/test_mtf_price_action.py tests/test_strategy_router.py` -> `18 passed`; local `./venv/bin/pytest -q tests/test_mtf_price_action.py tests/test_cohort_policy.py tests/test_paper_validation.py::test_probabilistic_candidate_rank_prefers_high_probability_edge_over_raw_score` -> `14 passed`; local compileall and `git diff --check` passed. Deployed MTF files/config/tests to VM; VM `tests/test_mtf_price_action.py tests/test_strategy_router.py` -> `18 passed`; VM compileall passed; config load prints `True 72.0`; restarted `screen` runtime, active pid `302589`; checked fresh log tail for `Traceback`/`ERROR`/`Unhandled`/`Telegram error` and found none.

- **Crypto-news scope narrowed to BTC/altcoins:** Tightened the hourly Telegram news brief so it filters out broad policy/banking/macro headlines unless the title explicitly references Bitcoin/BTC or altcoin/token scope (ETH, SOL, XRP, BNB, DOGE, ADA, AVAX, LINK, stablecoins/USDT/USDC/Tether, DeFi/on-chain/airdrop, etc.). Report title now reads `BTC & ALTCOIN URGENT NEWS — TOP 5`, and config comment reflects BTC/altcoin scope. Added regression coverage that broad policy headlines without BTC/altcoin scope are excluded. Verification: local `./venv/bin/pytest -q tests/test_crypto_news.py tests/test_main_reporting.py::test_maybe_send_crypto_news_report_runs_on_independent_interval` -> `5 passed in 8.55s`; local compileall for changed news files passed; `git diff --check` passed. Deployed `src/data/crypto_news.py`, `src/notifications/telegram.py`, `config/config.yaml`, and `tests/test_crypto_news.py` to VM; VM same focused tests -> `5 passed in 49.25s`; VM compileall passed; VM dry-run fetch returned 5 narrowed headlines focused on BTC/ETH/BNB/XRP/SOL/stablecoins; restarted `screen` runtime, active pid `283519`; checked log tail had no fresh `Traceback`/`ERROR`/`Unhandled`/`Telegram error`.

- **Independent hourly crypto-news Telegram brief:** Added `src/data/crypto_news.py`, a best-effort RSS/Atom fetcher that ranks urgent crypto headlines from configured public feeds without requiring a new API key. `TelegramNotifier.crypto_news_report()` now renders a separate `CRYPTO URGENT NEWS — TOP 5` report, and `NinjaTrader._maybe_send_crypto_news_report()` sends it on its own cadence before the scan loop work so it is independent from heartbeat, fund-manager, equity graph, and bootstrap reports. Config defaults: `telegram.crypto_news_interval_minutes=60`, `crypto_news_limit=5`, `crypto_news_timeout_seconds=8.0`, feeds = CoinDesk, Cointelegraph, and Decrypt. Verification: local `./venv/bin/pytest -q tests/test_crypto_news.py tests/test_main_reporting.py::test_maybe_send_crypto_news_report_runs_on_independent_interval tests/test_telegram_notifier.py::test_heartbeat_uses_floating_pnl` -> `5 passed in 7.98s`; local compileall for changed files and `git diff --check` passed. Deployed `src/data/crypto_news.py`, `src/notifications/telegram.py`, `src/main.py`, `config/config.yaml`, `tests/test_crypto_news.py`, and `tests/test_main_reporting.py` to VM; VM same focused suite -> `5 passed in 65.00s`; VM compileall passed; VM config check prints `60 5 3`; VM dry-run fetch returned `5` headlines; restarted `screen` runtime, active pid `282720`; checked log tail shows scanner/heartbeat active and no fresh `Traceback`/`ERROR`/`Unhandled`/`Telegram error` in the checked tail.

- **Dedicated short setup direction override:** `Scorer.score()` now runs the Phase D / LIQ_SWEEP dedicated short detector before final direction gating. When a valid dedicated short setup is present, it can override a generic `long`/`none` structure direction to `short`, so bearish sweep/SOW setups are not skipped before the reversal router can evaluate them. The existing distribution-regime and neutral-smart-money unblocks still apply only when the dedicated short setup is valid. Added `tests/test_scorer_short_setup_direction.py` regression coverage. Also corrected the positive-EV probe signal-confirmation regression so the weak p_win case is below the configured `0.50` floor. Verification: local `./venv/bin/pytest -q tests/test_scorer_short_setup_direction.py tests/test_short_strategies.py tests/test_strategy_router.py tests/test_paper_validation.py::test_paper_positive_ev_probe_allows_bounded_liquidity_sweep_short_below_score tests/test_paper_validation.py::test_paper_positive_ev_probe_allows_distribution_liquidity_sweep_neutral_sleeve tests/test_paper_validation.py::test_signal_confirmation_counts_positive_ev_probe_below_threshold` -> `29 passed in 9.17s`; local compileall for changed files passed; `git diff --check` passed. Deployed `src/scoring/scorer.py`, `tests/test_paper_validation.py`, and `tests/test_scorer_short_setup_direction.py` to VM; VM same focused suite -> `29 passed in 51.26s`; VM compileall passed; local/VM checksums match for deployed files. Restarted `screen` runtime, active pid `281764`; checked log tail shows heartbeat/scanner/market-context active and no fresh `Traceback`/`ERROR`/`Unhandled` in the checked tail.

- **Market-context operational reporting visibility:** Bootstrap audit and Telegram fund-manager reports now surface market-context attribution from the existing attribution report. `format_bootstrap_audit_lines()` adds an `Attribution market` block from `by_market_context`, and `TelegramNotifier.performance_report()` adds a `By Market` section with WR/PF/PnL/trade count or `_none_` when no market-context attribution exists. This makes the broad-market context integration visible in operational summaries, not only in the standalone attribution CLI. Verification: local `./venv/bin/pytest -q tests/test_bootstrap_audit.py tests/test_telegram_notifier.py::test_performance_report_uses_fund_manager_header tests/test_telegram_notifier.py::test_performance_report_shows_empty_open_positions tests/test_market_context.py tests/test_market_data_service.py tests/test_main_closed_trade_metadata.py tests/test_learner_state_loading.py` -> `24 passed in 10.27s`; local `./venv/bin/python -m compileall src tests` and `git diff --check` passed. Deployed `src/reporting/bootstrap.py`, `src/notifications/telegram.py`, `tests/test_bootstrap_audit.py`, and `tests/test_telegram_notifier.py` to VM; VM same focused suite -> `24 passed in 67.75s`; VM compileall passed. Restarted `screen` runtime, active pid `272786`; checked log tail shows scanner/heartbeat active, market context computed (`risk=mixed`, `rotation=mixed_rotation`, `conf=0.38`), and no fresh `Traceback`/`ERROR`/`Unhandled` in the checked tail. Current 4 open trades still predate market-context passport snapshots.

- **Shadow ML market-context parity:** `ShadowEngine.get_ml_trade_log()` now promotes shadow trade score metadata into the same top-level `TradeRecord` fields used by live learner/reporting rows: regime, sleeve, exit profile, session/hour/day, MFE/MAE, and broad market context (risk state, rotation state, BTC/ETH-BTC/BTC.D/TOTAL trends, confidence). This keeps shadow pretraining records aligned with the live closed-trade schema instead of only carrying context inside raw `scores`. Added regression coverage in `tests/test_market_context.py`. Verification: local `./venv/bin/pytest -q tests/test_market_context.py tests/test_market_data_service.py tests/test_main_closed_trade_metadata.py tests/test_learner_state_loading.py` -> `20 passed in 8.53s`; local `./venv/bin/python -m compileall src tests` and `git diff --check` passed. Deployed `src/backtest/shadow_engine.py` and `tests/test_market_context.py` to VM; VM same focused suite -> `20 passed in 36.75s`; VM compileall passed. Restarted `screen` runtime, active pid `272104`; checked log tail shows scanner/heartbeat active with market context cycles and no fresh `Traceback`/`ERROR`/`Unhandled` in the checked tail. Current 4 open trades still predate market-context passport snapshots, so their passports correctly remain without market context.

- **Market-context attribution reporting:** Attribution reporting now groups closed trade history by broad-market state: market risk state, rotation state, combined risk/rotation context, and BTC x ETH/BTC trend. The helpers read both top-level `TradeRecord` fields and legacy `scores` metadata, so live learner rows, restored rows, and shadow-pretraining rows can all surface market context in the same report. `compact_lines()` now includes a short `Market` summary, and `python -m src.report_attribution` renders the new market sections/headline fields. Added focused regression coverage in `tests/test_market_context.py`. Verification: local `./venv/bin/pytest -q tests/test_market_context.py tests/test_market_data_service.py tests/test_main_closed_trade_metadata.py tests/test_learner_state_loading.py` -> `19 passed in 8.84s`; local `./venv/bin/python -m compileall src tests` and `git diff --check` passed. Deployed `src/reporting/attribution.py`, `src/report_attribution.py`, and `tests/test_market_context.py` to VM; VM same focused suite -> `19 passed in 59.24s`; VM compileall passed. Restarted `screen` runtime, active pid `271458`. Post-restart runtime restored 4 pre-context open trades, resumed scanning, computed market context (`risk=mixed`, `rotation=mixed_rotation`, `conf=0.38`), and the checked log tail had no fresh traceback/error. Accidental root-level scp artifacts from deploy were removed.

- **Dataset market-context attribution wiring:** The ML dataset logger now records broad-market context columns on trade and near-miss rows: BTC trend, ETH/BTC trend, BTC.D trend, TOTAL trend, risk-on state, rotation state, and context confidence. `_extract_signal_fields()` reads the context from `SignalBreakdown.market_context` or falls back to `MarketSnapshot.market_context`, so parquet training rows align with the same context used by scoring, setup passports, and closed-trade learning. Added regression coverage in `tests/test_market_context.py`. Verification: local `./venv/bin/pytest -q tests/test_market_context.py tests/test_market_data_service.py tests/test_main_closed_trade_metadata.py tests/test_learner_state_loading.py` -> `18 passed in 8.18s`; local `./venv/bin/python -m compileall src tests` and `git diff --check` passed. Deployed `src/data/dataset_logger.py` and `tests/test_market_context.py` to VM; VM same focused suite -> `18 passed in 43.00s`; VM compileall passed; restarted `screen` runtime, active pid `270703`. Post-restart bot restored 4 open trades, resumed scanning, and fresh logs show market context still computed (`risk=mixed`, `rotation=mixed_rotation`, `conf=0.38`) with no fresh runtime traceback/error in the checked tail.

- **Restart-safe market-context passport persistence:** Entry setup passports now persist the broad-market context snapshot (`market_context` nested plus flat `market_risk_on_state`, `market_rotation_state`, BTC/ETH-BTC/BTC.D/TOTAL trends, and confidence) via `setup_passport_with_market_context()`. This closes the restart gap where `_pending_scores` could carry context during a live process, but restored open trades would lose it if the bot restarted before close. `enrich_closed_trade_scores()` now backfills market-context fields from the persisted passport when pending scores are absent, while preserving stronger pending-score values when present. Also aligned the `TradeManager` test fake risk signature with the sector exposure contract. Verification: local `./venv/bin/pytest -q tests/test_main_closed_trade_metadata.py tests/test_learner_state_loading.py tests/test_trade_manager_exit_profiles.py::test_trade_manager_persists_setup_passport tests/test_market_context.py tests/test_market_data_service.py` -> `18 passed in 9.35s`; local `./venv/bin/python -m compileall src tests` and `git diff --check` passed. Deployed `src/main.py`, `tests/test_main_closed_trade_metadata.py`, and `tests/test_trade_manager_exit_profiles.py` to VM; VM same focused suite -> `18 passed in 41.99s`; VM compileall passed; restarted `screen` runtime, active pid `270456`. Post-restart runtime restored 4 pre-context open trades (their passports correctly show no market context yet), loaded 7 learner trades, resumed scanning, and logs show market context computed again (`btc=flat`, `eth_btc=flat`, `risk=mixed`, `rotation=mixed_rotation`, `conf=0.38`).

- **Market-context learning metadata integration:** Closed-trade metadata now carries broad-market context from entry into `TradeRecord`: `market_risk_on_state`, `market_rotation_state`, BTC trend, ETH/BTC trend, BTC.D trend, TOTAL trend, and context confidence. `NinjaTrader` writes these fields into `_pending_scores` from `SignalBreakdown.market_context`, `enrich_closed_trade_scores()` preserves pending values and defaults restored pre-context trades to `unknown`, and `Learner._context_keys()` now adds market-context buckets independently from sector/session/time buckets. This means future closed trades can be attributed by BTC-led risk/rotation backdrop, not only pair/sector/session. Verification: local focused `./venv/bin/pytest -q tests/test_learner_state_loading.py tests/test_main_closed_trade_metadata.py tests/test_market_context.py tests/test_market_data_service.py tests/test_paper_validation.py::test_score_many_soft_penalizes_low_probability_negative_ev_candidate` -> `16 passed in 6.65s`; local `./venv/bin/python -m compileall src tests` and `git diff --check` passed. Deployed `src/main.py`, `src/learning/learner.py`, `tests/test_learner_state_loading.py`, and `tests/test_main_closed_trade_metadata.py` to VM; VM same focused suite -> `16 passed in 38.49s`; VM compileall passed; restarted `screen` runtime, active pid `269528`. Post-restart logs show `Learner state loaded: 6 trades`, market context still computed in scan cycles, and current legacy learner rows are backfilled with market fields as `unknown` until post-upgrade trades close.

- **Runtime integration verification:** Confirmed the VM runtime is active under `screen` session `268108.ninja-trader-vm` with bot pid `268111` running `./venv/bin/python -m src.main --mode paper`. Local and VM checksums match for the current integration files: `src/main.py`, `src/learning/learner.py`, `tests/test_learner_state_loading.py`, `tests/test_main_closed_trade_metadata.py`, `src/models/market_context.py`, `src/data/market_data.py`, and `src/scoring/scorer.py`, so no redeploy was needed. Runtime log shows market context is computed inside scan cycles (`btc=flat`, `eth_btc=flat`, `btc_d=unknown`, `total=unknown`, `risk=mixed`, `rotation=mixed_rotation`, `conf=0.38`). VM `data/open_trades.json` currently has 3 open paper trades (`IN`, `BAN`, `EIGEN`) and each persisted `setup.setup_passport` with setup type, sector, regime, expected path, and invalidation fields. Verification: local `./venv/bin/pytest -q tests/test_market_context.py tests/test_market_data_service.py tests/test_paper_validation.py::test_score_many_soft_penalizes_low_probability_negative_ev_candidate tests/test_paper_validation.py::test_paper_threshold_calibration_collects_mid_30s_trending_samples tests/test_main_closed_trade_metadata.py tests/test_learner_state_loading.py` -> `15 passed in 7.57s`; VM same suite -> `15 passed in 38.77s`; local `./venv/bin/python -m compileall src tests` passed.

- **Learner legacy-state migration for stable edge feedback:** Added load-time context backfill for legacy/restored `TradeRecord` rows in `src/learning/learner.py`. Older learner records now populate `session`, `hour_of_day`, `day_of_week`, `asset`, and `sector` plus matching `scores` metadata from `opened_at` and symbol sector mapping, then persist the migrated state. This closes the remaining feedback-loop gap where pre-patch closed trades could not participate in sector/session/hour/day context learning. Verification: local `./venv/bin/pytest -q tests/test_learner_state_loading.py tests/test_main_closed_trade_metadata.py tests/test_strategy_lifecycle.py tests/test_bootstrap_audit.py tests/test_cohort_policy.py` -> `23 passed in 6.69s`; VM same focused suite -> `23 passed in 39.26s`; local/VM compileall passed. VM learner state now shows `6` trades with context fields populated (`session=asia`, `hour=4`, `day=6`, sector in both top-level and `scores`); restarted `screen` runtime, active pid `268111`, boot log reports `Learner state loaded: 6 trades`; runtime remains active with market context, EdgeDetector, AlphaDecay, and bandit hydrated from the same trade history.

- **Restored-trade learning metadata fallback:** Fixed a restart integration gap where restored open trades could close without `_pending_scores`, causing `TradeRecord` to lose `session`, `hour_of_day`, `day_of_week`, regime code, and sector context despite `setup_passport` being persisted. Added `enrich_closed_trade_scores()` in `src/main.py` to backfill closed-trade metadata from `trade.opened_at`, symbol, setup, and setup passport before the Learner records outcomes. Also capped `Learner` context adjustment per signal key so overlapping sector/session/hour/day buckets cannot over-amplify score weights. Verification: local `./venv/bin/pytest -q tests/test_main_closed_trade_metadata.py tests/test_learner_state_loading.py tests/test_strategy_lifecycle.py tests/test_bootstrap_audit.py tests/test_cohort_policy.py` -> `22 passed in 6.87s`; VM same focused suite -> `22 passed in 50.50s`; local and VM compileall passed; deployed `src/main.py`, `src/learning/learner.py`, and `tests/test_main_closed_trade_metadata.py`; restarted `screen` runtime, active pid `267202`, no fresh traceback/error in checked boot log tail.

- **VM dependency sync after disk expansion:** Installed missing lock/dev validation packages in VM venv with `--no-cache-dir`: `xgboost==3.2.0`, `nvidia-nccl-cu12==2.30.4`, and `pytest==9.0.3`. Import check confirms XGBoost and pytest import; `nvidia.nccl` package is present, but no `nvidia-smi` binary was found, so this is package alignment, not GPU-driver enablement. VM focused tests now run and pass: `./venv/bin/pytest -q tests/test_learner_state_loading.py tests/test_strategy_lifecycle.py tests/test_bootstrap_audit.py tests/test_cohort_policy.py` -> `20 passed in 2.74s`; VM compileall passed. Disk remains healthy after install: root about `70%` used / `2.8G` free, `/mnt/ninja-data` about `5%` used / `19G` free. Runtime bot stayed active as `./venv/bin/python -m src.main --mode paper`; latest heartbeat shows `equity=$69.40`, drawdown `0.9%`, open `4`.

- **Learner context feature wiring:** `TradeRecord` now carries `hour_of_day` and `day_of_week` alongside existing `session`, `asset`, and `sector`; `main.py` stores UTC hour/day and passport sector in pending scores before creating closed-trade records. `Learner` now applies conservative sector/session/hour-bucket/day context modulation on top of existing score-weight and `regime|direction` learning, with no new admission/risk gates. Verification: local `./venv/bin/pytest -q tests/test_learner_state_loading.py` -> `3 passed`; local `./venv/bin/pytest -q tests/test_strategy_lifecycle.py tests/test_bootstrap_audit.py tests/test_cohort_policy.py` -> `17 passed`; local compileall passed. Deployed `src/learning/learner.py`, `src/main.py`, and `tests/test_learner_state_loading.py` to VM; VM compile passed; VM lacks `pytest` in venv so focused pytest could not run there. Restarted `screen` session `ninja-trader-vm`; active runtime `./venv/bin/python -m src.main --mode paper` pid `264352`; no fresh traceback/error in checked `boot.log` tail.

- **VM disk expansion / root cleanup:** Attached disk `google-disk-20260524-043158` (`/dev/sdc`, 20G) was formatted ext4 and mounted permanently at `/mnt/ninja-data` via UUID `10cabf62-15ba-4cb6-a090-30a78b2062c0` in `/etc/fstab`. Moved old non-runtime home archives (`Futures`, `Futures.old.2026-05-14_225446`, `Futures_backup`, `Futures_untracked_backup_20260521`, `ninja_trader.bundle`) to `/mnt/ninja-data/home-archive`; active runtime repo remains `/home/kiosarwin/ninja_trader`. Cleaned system logs by vacuuming journald to ~80M, truncating stale `/var/log/syslog.1`, and adding journald limits in `/etc/systemd/journald.conf.d/99-size-limit.conf`. Root disk improved from about `90%` used / `948M` free to about `64%` used / `3.4G` free; `/mnt/ninja-data` is about `5%` used / `19G` free. Bot process stayed running as `./venv/bin/python -m src.main --mode paper`.

- **Hard Reset & Bot Intelligence Upgrade:**
    - Performed hard reset to `$70` equity (token: `intelligence_upgrade_2026_05_24_v1`).
    - **Implemented Sector Awareness:** Bot now tracks and limits exposure by sector (AI, Meme, L1/L2, etc.).
    - **Sector Risk Guard:** Max sector exposure capped at `2.5%` to prevent correlated cascades.
    - **Probabilistic Ranking Boost:** Scorer is now more sensitive to EV (`total_score` rewarded higher for positive net edge).
    - **Policy Hardening:** `CohortPolicy` now blocks entire sectors if they show negative momentum (2 losses in 5 trades).
    - **Fund Manager (Jim) Optimization:** Relaxed Jim's veto in paper mode to allow cleaner edge discovery.
    - **Data Layer Upgrade:** `TradeRecord` and `ShadowEngine` now capture sector attribution for future ML training.

- Fixed the latest VM `G-REG` bottleneck for bounded positive-EV distribution probes. Runtime showed `NAORIS/USDT:USDT` repeatedly rejected at `G-REG` with `regime=distribution`, `sm=liquidity_sweep`, smart-money score `80`, `p_win≈0.510`, and `EV≈+0.68%`; the core scorer had zeroed its gated score before the paper probe could run. `SignalBreakdown` now carries `pre_gate_score`; `_paper_positive_ev_probe_mode()` can use that score basis and can unblock the regime gate only when the existing positive-EV probe contract passes (paper mode, EV ok, smart-money ok, allowed direction/regime/phase/sleeve, min EV, min p_win, min SM score, score floor, stress guard). Candidate rotation also uses this same score basis so gate-suppressed probes are comparable to open positions. Added `test_paper_positive_ev_probe_uses_pre_gate_score_for_distribution_gate`. Verification: local `./venv/bin/pytest -q tests/test_paper_validation.py::test_paper_positive_ev_probe_allows_distribution_liquidity_sweep_neutral_sleeve tests/test_paper_validation.py::test_paper_positive_ev_probe_uses_pre_gate_score_for_distribution_gate tests/test_paper_validation.py::test_paper_positive_ev_probe_blocks_low_probability_or_deep_score_gap tests/test_position_rotation.py` -> `7 passed in 36.62s`; local compileall passed. Deployed `src/main.py`, `src/scoring/scorer.py`, and `tests/test_paper_validation.py` to VM, cleaned accidental root deploy artifacts, restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `134207`. Post-restart evidence: NAORIS now logs `paper validation positive-EV probe unblocked regime gate`, `score_basis=36.0`, bypasses paper scope, reaches `risk_guard max_open_trades (5) reached`, and rotation correctly skips because candidate advantage is `-6.2 < 0.0` versus the weakest eligible open trade. Chaos candidates remain rejected at `G-REG`.

- Added positive-EV-probe-aware position rotation so bounded alpha probes are not blocked by the generic `min_candidate_score=72` rotation threshold. Rotation now detects candidates that pass `_paper_positive_ev_probe_mode()` and applies separate config floors: `positive_ev_probe_min_candidate_score=35.0`, `positive_ev_probe_min_score_advantage=0.0`, and `positive_ev_probe_min_candidate_quality=0.0`; all existing rotation budget, victim age, TP1/MFE/gain protection, p_win/EV, and max-adverse filters remain active. Open-trade rotation strength now penalizes missing setup passports and negative entry EV, so legacy pre-passport negative-EV positions are weaker rotation victims than fresh contract-backed trades. Added `test_position_rotation_uses_probe_thresholds_for_positive_ev_distribution_sweep`. Verification: local `./venv/bin/pytest -q tests/test_position_rotation.py tests/test_paper_validation.py::test_paper_positive_ev_probe_allows_distribution_liquidity_sweep_neutral_sleeve` -> `5 passed in 54.88s`; local compileall and `git diff --check` passed. Deployed `src/main.py`, `config/config.yaml`, and `tests/test_position_rotation.py` to VM; VM compile passed; VM config confirms probe rotation floors `35.0 0.0 0.0`; restarted `ninja-vm`, active runtime `./venv/bin/python -m src.main` pid `133009`. Post-restart runtime is healthy with restored `open=5`; market conditions changed before a live probe rotation fired, so the behavior is currently proven by focused regression and the deployed config/runtime health, not by an executed rotation.

- Extended the bounded `positive_ev_probe` lane to include distribution-regime liquidity-sweep shorts after runtime evidence showed `NAORIS/USDT:USDT` had all core gates passing (`short`, `distribution`, `liquidity_sweep`, smart-money score `80`, p_win about `0.51`, EV about `+0.68%`) but was rejected at `SCORE` / `PAPER_SCOPE` because the setup still carried residual `neutral` sleeve labeling. Config now allows `positive_ev_probe_allowed_regimes=[trending_expansion, distribution]` and lowers `positive_ev_probe_min_p_win` from `0.53` to `0.50`, while keeping min EV `+0.50%`, min smart-money score `75`, short-only direction, liquidity-sweep phase, score floor, EV observation floor, and chaos regime blocks intact. Added `test_paper_positive_ev_probe_allows_distribution_liquidity_sweep_neutral_sleeve` and adjusted the low-pwin block regression to `0.49`. Verification: local focused `./venv/bin/pytest -q tests/test_paper_validation.py::test_paper_positive_ev_probe_allows_bounded_liquidity_sweep_short_below_score tests/test_paper_validation.py::test_paper_positive_ev_probe_allows_distribution_liquidity_sweep_neutral_sleeve tests/test_paper_validation.py::test_paper_positive_ev_probe_blocks_low_probability_or_deep_score_gap tests/test_paper_validation.py::test_paper_threshold_calibration_collects_mid_30s_trending_samples` -> `4 passed in 49.25s`; local compileall and diff check passed. Deployed `config/config.yaml` and `tests/test_paper_validation.py` to VM; VM compile passed; VM config confirmed `probe 0.5 [trending_expansion, distribution]`; restarted `ninja-vm`, active runtime `./venv/bin/python -m src.main` pid `131718`. Runtime evidence after restart: NAORIS now logs `paper validation positive-EV probe enabled`, bypasses scope, and advances to `risk_guard max_open_trades (5) reached`; rejection parquet confirms latest NAORIS stage moved from `score_threshold` to `risk_guard`.

- Calibrated paper-validation score throughput after rejection telemetry showed `SCORE` dominating no-signal cycles. `paper_validation.threshold_relaxation` is now `15.0` and `min_score_floor` is `35.0`, making effective paper thresholds `35` for `trending_expansion` and `37` for `distribution` while leaving `G-EV` observation floor and `G-REG` chaos blocks intact. Added `test_paper_threshold_calibration_collects_mid_30s_trending_samples`. Verification: local focused `./venv/bin/pytest -q tests/test_paper_validation.py::test_paper_threshold_calibration_collects_mid_30s_trending_samples tests/test_paper_validation.py::test_paper_ev_observation_floor_blocks_low_probability_deep_negative_ev tests/test_paper_validation.py::test_fund_manager_does_not_hard_veto_bad_day_loss_streak_in_paper_validation` -> `3 passed in 44.02s`; local compileall and `git diff --check` passed. Deployed `config/config.yaml` and `tests/test_paper_validation.py` to VM; VM config check confirmed `relax=15.0`, `floor=35.0`, `trend_thresh=35.0`, `dist_thresh=37.0`, EV floor still `True 0.3 0.75`; VM compile passed; restarted `ninja-vm`, active runtime `./venv/bin/python -m src.main` pid `130419`. Runtime reached `open=5` and the fresh `ETH/USDT:USDT` paper trade has a persisted `setup_passport`.

- Fixed paper-validation FundManager bottleneck observed on VM: `Jim VETOED trade ... consecutive losses on a bad day` was still a hard veto even though cooldown was disabled and paper mode is meant to keep collecting controlled samples. `FundManager._veto_check()` now keeps the bad-day loss-streak hard veto outside paper-validation mode, while existing paper score/EV/regime gates remain active. Verification: local focused `./venv/bin/pytest -q tests/test_paper_validation.py::test_fund_manager_does_not_veto_drawdown_only_in_paper_validation tests/test_paper_validation.py::test_fund_manager_does_not_hard_veto_bad_day_loss_streak_in_paper_validation tests/test_paper_validation.py::test_fund_manager_keeps_bad_day_loss_streak_veto_outside_paper_validation` -> `3 passed in 44.99s`; local compileall and `git diff --check` passed. Deployed `src/models/fund_manager.py` and `tests/test_paper_validation.py` to VM, VM compile passed, restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `129705`. Post-restart scanner/heartbeat resumed with `open=4`; latest rejections are now normal admission gates (`SCORE`, `G-EV`, `G-REG`), not FundManager bad-day veto.

- Added probabilistic fallback risk sizing to `KellySizer`. When an EV result exists but is not statistically tradeable / Kelly-positive yet, sizing no longer falls straight back to the full default risk. The fallback risk is now scaled by posterior p(win), conservative p(win), EV net, conservative EV, and sample confidence, bounded so weak negative-posterior probes stay small while stronger positive-posterior probes can still be explored. Regression coverage: `test_calculate_setup_scales_unproven_ev_fallback_by_probability` proves a p_win 0.22 / EV -1.0 setup risks materially less than default, while a p_win 0.58 / EV +0.80 probe sizes higher but remains capped. Verification: local focused risk tests -> `5 passed in 13.26s`; local probabilistic admission/scoring slice -> `2 passed in 54.53s`; local compileall for `src/risk/kelly_sizer.py` and `tests/test_risk_manager.py` passed; `git diff --check -- src/risk/kelly_sizer.py tests/test_risk_manager.py` passed. Deployed `src/risk/kelly_sizer.py` to VM, checksum matched local, VM compile passed, recreated/restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `127964`, heartbeat/scan resumed with no fresh startup error in checked tail.

- Added Bayesian/scaled symbol-priority scoring to `CohortPolicy.recommend_symbols()`. Scanner recommendations now bucket PnL per symbol, ignore legacy/non-alpha context, rank primary-edge symbols first, and use a beta-binomial lower-bound plus sample-confidence multiplier so one lucky winner does not outrank a smaller but more consistent alpha-context sample. Verification: local `./venv/bin/pytest -q tests/test_cohort_policy.py::test_recommend_symbols_prioritizes_primary_edge_symbols_before_generic_winners tests/test_cohort_policy.py::test_recommend_symbols_does_not_prioritize_losing_primary_edge_symbol tests/test_cohort_policy.py::test_recommend_symbols_ignores_legacy_unknown_context_winners tests/test_cohort_policy.py::test_recommend_symbols_prefers_consistent_symbol_over_one_lucky_win` -> `4 passed in 0.94s`; local compileall for `src/models/cohort_policy.py` and `tests/test_cohort_policy.py` passed; `git diff --check -- src/models/cohort_policy.py tests/test_cohort_policy.py` passed. Deployed `src/models/cohort_policy.py` to VM, checksum matched local, VM compile passed, recreated/restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `126681`, heartbeat/scan resumed with no fresh startup error in checked tail.

- Fixed scanner learned-priority ordering so cohort/primary-edge symbol recommendations actually move to the front of the scan queue. Previously `PairScanner.scan()` skipped priority symbols already present in the volume-ranked result, so high-volume historically useful symbols were not promoted despite `CohortPolicy.recommend_symbols()`. The scanner now builds a de-duplicated priority front, validates market/price, includes valid below-volume priority symbols, then appends remaining volume-ranked symbols. This moves probabilistic/learned edge evidence earlier in the pipeline before the top-pairs fetch cap. Verification: local `./venv/bin/pytest -q tests/test_scanner.py tests/test_cohort_policy.py::test_recommend_symbols_prioritizes_primary_edge_symbols_before_generic_winners tests/test_cohort_policy.py::test_recommend_symbols_does_not_prioritize_losing_primary_edge_symbol tests/test_cohort_policy.py::test_recommend_symbols_ignores_legacy_unknown_context_winners` -> `6 passed in 22.53s`; local compileall for `src/scanner/scanner.py` and `tests/test_scanner.py` passed; `git diff --check -- src/scanner/scanner.py tests/test_scanner.py` passed. Deployed `src/scanner/scanner.py` to VM, VM checksum matched local, VM compile passed, restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `126013`; scanner/heartbeat continued and checked log tail had no fresh ERROR/Traceback/Unhandled.
- Extended the probabilistic math upstream into scan scoring. `Scorer.score_many()` now applies a bounded `probability_score_mult` after strategy routing, using posterior p(win), conservative p(win), EV net, conservative EV, and confidence to softly penalize low-probability negative-EV candidates and modestly reward better posterior setups. This keeps EV observable when the hard gate is off, but prevents raw technical score from dominating scan priority. Regression: `test_score_many_soft_penalizes_low_probability_negative_ev_candidate` proves a 68-score candidate with p_win 0.22 / EV -1.00 is pushed below threshold while a lower raw-score positive-posterior candidate ranks first. Verification: local focused tests passed; local `./venv/bin/pytest -q tests/test_paper_validation.py` -> `38 passed in 45.31s`; local compileall for `src/scoring/scorer.py` and `tests/test_paper_validation.py` passed; `git diff --check -- src/scoring/scorer.py tests/test_paper_validation.py` passed. Deployed `src/scoring/scorer.py` to VM, VM checksum matched local, VM compile passed, restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `125854`. Runtime evidence after restart: low-pwin IN/USDT no longer dominated top scan (`score` dropped from prior 67.8 to roughly 30-35 with p_win ~0.22 / EV ~-1.00), scanner/heartbeat continued, and checked log tail had no fresh ERROR/Traceback/Unhandled.
- Added probabilistic candidate ranking for eligible admissions. `NinjaTrader._probabilistic_candidate_rank()` now blends EV posterior p(win), ML p(win), and setup quality in log-odds space, pulls the posterior toward conservative p(win) when sample confidence exists, and ranks by expected log-growth plus EV/conservative-EV/setup/cohort terms. This does not loosen gates; it changes slot allocation among already eligible candidates so raw score alone cannot outrank materially better probability/EV setups. Verification: local `./venv/bin/pytest -q tests/test_paper_validation.py` -> `37 passed in 52.06s`; local compileall for `src/main.py` and `tests/test_paper_validation.py` passed; `git diff --check -- src/main.py tests/test_paper_validation.py` passed. Deployed `src/main.py` to VM, VM compile passed, checksum matched local, restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `124770`; initial post-restart log showed scanner/heartbeat continuing and no fresh ERROR/Traceback/Unhandled in checked tail.
- Temporarily disabled the EV hard gate after live scan evidence showed most candidates carrying negative point EV while existing open positions were floating positive. The conservative statistical EV implementation remains in code/config for later reactivation, but `ev_model.gate_enabled=false` makes EV observe-only again so it does not hard-block admission. Verification: local compileall for `src/models/ev_model.py` and `src/scoring/scorer.py` passed; `git diff --check -- config/config.yaml` passed. Deployed `config/config.yaml` and `session.md` to VM; VM compile passed; VM config confirms `ev_gate=False` and `stat_gate=True`; restarted `ninja-vm`, active runtime `./venv/bin/python -m src.main` pid `119386`.
- Activated EV with a conservative statistical gate instead of a raw point-estimate gate. `EVModel` now publishes `conservative_p_win` from a beta-binomial one-sided lower bound and `conservative_ev_net_pct` after payoff haircut, loss inflation, and extra cost buffer. With `ev_model.statistical_gate_enabled=true`, `Scorer` no longer lets small-sample positive point EV pass; matched setup samples must reach `min_trades_for_ev=20` and `EVResult.is_tradeable` must pass the conservative statistical edge. Config now has `ev_model.gate_enabled=true`, `confidence_z=1.0`, `payoff_haircut=0.85`, `loss_inflation=1.10`, `cost_buffer_pct=0.05`, and `min_conservative_ev_pct=0.0`. Verification: local `./venv/bin/pytest -q tests/test_position_rotation.py tests/test_ev_model_fixes.py tests/test_paper_validation.py` -> `46 passed in 55.23s`; local compileall for changed EV/scorer/tests passed; `git diff --check` passed. Deployed `config/config.yaml`, `src/models/ev_model.py`, `src/scoring/scorer.py`, EV/scorer tests, and `session.md` to VM; VM compile passed; VM config confirms `ev_gate=True`, `stat_gate=True`, `min_trades=20`, `z=1.0`, `payoff_haircut=0.85`, `loss_inflation=1.1`, `cost_buffer=0.05`. Restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `118607`, startup restored 4 paper trades and scanner resumed without fresh ERROR/Traceback in checked log tail.
- Applied the aggressive-but-measured paper guard after the IN/USDT stop-loss review. `risk.min_risk_usd` is now `0.50` so AdaptiveBrain/FundManager scale-down is not overridden as hard on the `$70` paper account. Added `paper_validation.ev_observation_floor_*`: while `ev_model.gate_enabled=false`, paper admission now blocks only the worst combination of low probability and deep negative EV (`p_win < 0.30` and `ev_net < -0.75%`), preserving positive-EV probes and normal EV-gated behavior. Position-rotation pre-close logging now says the victim is pending downstream gates before any close, avoiding the misleading `will close` wording. Verification: local `./venv/bin/pytest -q tests/test_paper_validation.py tests/test_position_rotation.py` -> `37 passed in 44.70s`; local `./venv/bin/python -m compileall src/main.py tests/test_paper_validation.py` passed; `git diff --check` passed. Deployed `config/config.yaml`, `src/main.py`, `tests/test_paper_validation.py`, and `session.md` to VM; VM compile passed; VM config confirms `min_risk=0.5`, `ev_gate=False`, and EV floor `True 0.3 0.75`; restarted `ninja-vm`, active runtime `./venv/bin/python -m src.main` pid `117791`, startup restored 4 open paper trades and showed no fresh ERROR/Traceback in the checked log tail.
- Added aggressive-but-bounded position rotation for full-slot conditions. New `position_rotation` config lets the bot replace a weak aged open trade only when a new candidate has already passed the admission path up to ready-open, the risk block is specifically `max_open_trades`, candidate score/quality/p_win/EV meet floors, candidate advantage over the weakest eligible open trade is large enough, and cycle/day rotation budgets allow it. Winners are protected after TP1, strong MFE, or meaningful unrealized gain; deeply adverse positions near full loss are not rotated by this path. `TradeManager` now exposes `open_trades` and `close_for_rotation()` so the close uses the normal close pipeline/accounting. Verification: local `./venv/bin/pytest -q tests/test_position_rotation.py tests/test_market_context.py tests/test_market_data_service.py tests/test_trade_manager_exit_profiles.py::test_trade_manager_persists_setup_passport` -> `11 passed in 33.82s`; local compile for `src/main.py`, `src/execution/trade_manager.py`, and `tests/test_position_rotation.py` passed; `git diff --check` passed.
- Added the first broad-market context layer. `MarketDataService` now fetches a cached BTC/ETH context once per scan cycle, derives `btc_trend`, `eth_btc_trend`, optional `btc_d_trend` / `total_trend`, `risk_on_state`, `rotation_state`, and `confidence`, attaches it to every `MarketSnapshot`, and keeps backward-compatible `btc_state` / `btcd_state` hints for the EV/P(win) context. `Scorer` applies this as a soft direction-aware multiplier, not a hard gate. `BTC.D` and `TOTAL` are configured as optional blank symbols until an external/index feed is available, so fallback remains graceful. Verification: local focused `./venv/bin/pytest -q tests/test_market_context.py tests/test_market_data_service.py tests/test_paper_validation.py::test_score_many_keeps_neutral_fallback_conservative tests/test_paper_validation.py::test_score_many_keeps_alpha_sleeves_anchored_to_institutional_score` -> `9 passed in 31.26s`; local compileall for changed market-context files passed; `git diff --check` passed. Runtime note before deploy: VM bot was healthy at pid `111419`, heartbeat `equity=$68.60 drawdown=2.0% daily_pnl=-2.0% open=5`; all 5 restored open trades still have empty `setup_passport`, so fresh passport persistence remains unproven until one slot frees and a new trade opens.
- Upgraded setup passport from entry label into a persisted trade contract. `TradeSetup` now carries `setup_passport`, `main.py` passes the router passport into every real/shadow setup, `open_trades.json` persists the full passport for new positions, floating PnL surfaces `expected_path` / `invalidation`, and `TradeManager` has a configurable `exit.passport_monitor` that can close pre-TP1 trades when the entry path fails by R-based thresholds (`passport_failed_continuation`, `passport_failed_breakout`, `passport_failed_reversal`). Added regression coverage for passport persistence and failed-breakout passport exit. Verification: local `./venv/bin/python -m compileall src tests` passed; local `./venv/bin/pytest -q tests/test_trade_manager_exit_profiles.py tests/test_risk_manager.py tests/test_strategy_router.py tests/test_paper_validation.py` -> `62 passed in 42.04s`; `git diff --check` passed. Deployed `config/config.yaml`, `src/risk/risk_manager.py`, `src/execution/trade_manager.py`, `src/main.py`, and `tests/test_trade_manager_exit_profiles.py` to VM; VM compile passed; VM config has `passport_monitor.enabled=True` and `TradeSetup.setup_passport`; restarted `ninja-vm`, active runtime `./venv/bin/python -m src.main` pid `111419`, restored 5 existing open trades. Existing restored trades predate the persisted passport field and therefore have empty passports; newly opened trades will persist full passports.
- Fixed Telegram balance/equity PNG layout overlap. `_build_equity_graph_png()` now reserves a dedicated top header band for title/meta/latest metrics, moves the main legend to the upper-right, keeps drawdown legend on the left, and moves latest DD text to the right so labels no longer stack on top of each other. Verification: rendered a local sample PNG (`/tmp/equity_graph_layout_check.png`), `./venv/bin/pytest -q tests/test_telegram_notifier.py::test_equity_graph_report_sends_png_photo` -> `1 passed`, full notifier tests -> `29 passed in 16.77s`, compileall passed locally and on VM, `git diff --check` passed. Deployed `src/notifications/telegram.py` to VM and restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `105921`; startup restored 4 open paper trades and no new Telegram/PNG/runtime errors were found in the post-restart log check.
- Runtime hardening: fixed scanner ticker-fetch transient failures so `PairScanner.scan()` catches `fetch_tickers()` exceptions, logs `Scanner ticker fetch failed`, and returns an empty list for that cycle instead of letting Binance `ticker/24hr` network errors bubble into `Unhandled error in main loop` / runtime error count. Added `tests/test_scanner.py` regression coverage. Verification: local full `./venv/bin/pytest -q` -> `281 passed in 71.84s`; local focused `./venv/bin/pytest -q tests/test_scanner.py tests/test_api_call_timeout.py tests/test_market_data_service.py tests/test_paper_validation.py` -> `60 passed in 33.31s`; `git diff --check` passed; local/VM compileall for changed scanner/test passed. Deployed `src/scanner/scanner.py` and `tests/test_scanner.py` to VM, restarted `ninja-vm`; active runtime `./venv/bin/python -m src.main` pid `104842`; startup restored 3 open paper trades and loaded learner/shadow/adaptive state; post-restart log check found no new `ERROR`/`Traceback`/`Unhandled error`.
- Resume verification: local worktree remains dirty with the in-flight passport/router/EV/Telegram/market-data changes. Local config check matches intended paper settings (`paper_starting_equity=70`, `min_risk_usd=0.75`, `max_direction_risk_pct=5.5`, normalized `max_open_trades=5`, `min_score_threshold=42`, `ev_model.gate_enabled=False`, `telegram.parse_mode=''`). VM runtime is active in `tmux` session `ninja-vm` with `./venv/bin/python -m src.main` pid `102393`; VM config matches the same key settings. Latest observed VM heartbeat: equity `$68.60`, drawdown `2.0%`, daily PnL `-2.0%`, open `2`; scanner continues to cycle. Local verification after resume: `git diff --check` passed, `./venv/bin/python -m compileall src tests` passed, and `./venv/bin/pytest -q tests/test_strategy_router.py tests/test_paper_validation.py tests/test_cohort_policy.py tests/test_strategy_lifecycle.py tests/test_bootstrap_audit.py tests/test_market_data_service.py tests/test_telegram_notifier.py` -> `93 passed in 39.32s`. No deploy or restart was performed in this resume step because VM was already healthy and no code logic was changed.
- Cleaned Telegram Markdown parse failures. `TelegramNotifier` now sends plain text by default and only uses `parse_mode` when explicitly configured via `telegram.parse_mode`; this avoids Telegram legacy Markdown breaking on dynamic futures labels such as `trend_following` / `trending_expansion`. Added `telegram.parse_mode: ""` to config and updated notifier tests. Verification: local `./venv/bin/pytest -q tests/test_telegram_notifier.py` -> `29 passed in 25.20s`; local and VM compileall passed. Deployed `src/notifications/telegram.py`, `config/config.yaml`, and `tests/test_telegram_notifier.py` to VM; restarted `ninja-vm`; active process `./venv/bin/python -m src.main` pid `102393`; VM notifier instance reports `parse_mode=''`; startup restored 3 open paper trades and heartbeat shows `open=3`.

- Made setup passport explicit for footprint-based futures pattern discovery. Added `src/models/strategy_passport.py` with `SetupPassport` fields for setup type, sector/narrative bucket, rotation state, crowding/funding state, microstructure state, participation, expected path, invalidation, and hold profile. `StrategyRouter` now emits the passport on every decision; `SignalBreakdown`, dataset logging, and trade `scores` persist passport fields so cohort learning can distinguish market-maker/flow footprints instead of mixing all score-positive trades. Added lightweight sector/rotation classification for majors, L1/L2, meme, AI, DePIN, modular, BTC ecosystem, and microcap symbols. Verification: local `./venv/bin/pytest -q tests/test_strategy_router.py tests/test_paper_validation.py` -> `46 passed in 41.30s`; local `./venv/bin/pytest -q tests/test_strategy_router.py tests/test_cohort_policy.py tests/test_strategy_lifecycle.py` -> `29 passed in 2.72s`; compileall passed locally and on VM. Deployed passport/router/scorer/dataset/main/test files to VM, cleaned accidental deploy artifacts under `src/data/`, restarted `ninja-vm`; active process `./venv/bin/python -m src.main` pid `101581`; VM restored 2 open paper trades (`CHILLGUY`, `BEAT`); after the first post-passport loop it opened `IN/USDT:USDT` long trend-following at `0.08156`, so latest VM open state is 3 paper trades (`CHILLGUY` short, `BEAT` long, `IN` long).
- User requested closing all open trades. Stopped VM bot, backed up `data/open_trades.json`, `data/risk_state.json`, and `data/learner_state.json` with `.manual_close_all_20260523_052015.bak`, manually closed 5 paper trades from persisted state at entry/flat price with round-trip fee accounting (`manual_close_all`), total recorded PnL `-$0.076664`, cleared open trade state and risk heat, then restarted `ninja-vm`. Active process `./venv/bin/python -m src.main` pid `99754`. Verification: `data/open_trades.json` reports `open 0`; latest VM heartbeat shows `equity=$69.09 drawdown=1.3% daily_pnl=-1.3% open=0`. Manual closed records appended to `data/manual_closed_trades.jsonl`.
- Sharpened long/short strategy routing after EV gate disable. `StrategyRouter` now assigns a setup passport (`setup_type`, `quality_score`) and requires futures-style setup quality before alpha sleeves: directional alignment from feature vectors, no hard microstructure contradiction (for example long into bearish BOS + sell flow), and mandatory volume/OI participation for trend continuation. Compression breakouts require cleaner alignment; reversals can stay contrarian but cannot contradict structure. Config added `trend_require_participation=true`, `trend_min_alignment=0.54`, `breakout_min_alignment=0.57`, and `reversal_min_alignment=0.45`. Verification: local `./venv/bin/pytest -q tests/test_strategy_router.py tests/test_paper_validation.py tests/test_cohort_policy.py tests/test_strategy_lifecycle.py` -> `58 passed in 37.74s`; local `python -m compileall src/models/strategy_router.py tests/test_strategy_router.py` passed. Deployed `config/config.yaml`, `src/models/strategy_router.py`, `tests/test_strategy_router.py`, and `session.md` to VM; VM compile passed; config check shows `ev_model.gate_enabled=False`, `trend_require_participation=True`, alignment floors `0.54/0.57/0.45`; restarted `ninja-vm`; active process `./venv/bin/python -m src.main` pid `99572`. Runtime scan is healthy with `open=5`; new candidates show EV gate still disabled (`X✓`) but additional entries are currently blocked by `max_open_trades (5) reached`, as expected.
- User requested temporarily disabling the EV gate until an edge is found. Added `ev_model.gate_enabled: false`; EV is still computed and logged for telemetry/sizing inputs, but `Scorer` no longer lets EV block admission while the toggle is false. Paper EV stress tightening now also stays inactive when the EV gate is disabled, so it cannot silently re-enable EV blocking during drawdown/loss-streak stress. Verification: local `./venv/bin/pytest -q tests/test_paper_validation.py` -> `31 passed in 31.56s`; local `python -m compileall src/main.py src/scoring/scorer.py tests/test_paper_validation.py` passed. Deployed `config/config.yaml`, `src/main.py`, `src/scoring/scorer.py`, and `tests/test_paper_validation.py` to VM; VM compile passed; restarted `ninja-vm`; active process `./venv/bin/python -m src.main` pid `97803`. VM config checks show `ev_model.gate_enabled=False`, normalized max open `5`, and token `edge_recalibration_evstrict_2026_05_23`. Fresh VM scan confirmed gates `R✓S✓X✓`, no fresh `G-EV` blocking, and opened 5 paper trades despite negative EV telemetry being recorded for analysis.
- Added and deployed a bounded paper-only `positive_ev_probe` lane for strong positive-EV liquidity-sweep shorts that are below the score threshold but still meet strict guards: EV net >= `+0.50%`, p_win >= `0.53`, score >= `max(30, threshold-12)`, smart-money score >= `75`, direction `short`, regime `trending_expansion`, phase `liquidity_sweep`, blocked during paper stress tightening. Confirmation now counts this same probe path so score-below-threshold probes are not silently starved before admission. Verification: focused local `./venv/bin/pytest -q tests/test_paper_validation.py tests/test_strategy_lifecycle.py tests/test_cohort_policy.py tests/test_bootstrap_audit.py tests/test_market_data_service.py` -> `47 passed in 35.92s`; local `python -m compileall src tests` passed; VM `./venv/bin/python -m compileall src/main.py tests/test_paper_validation.py` passed. Deployed `config/config.yaml`, `src/main.py`, and `tests/test_paper_validation.py`; restarted `ninja-vm`; active VM process `./venv/bin/python -m src.main` pid `93672`. Runtime evidence: first fresh scan opened paper `SHORT DYM/USDT:USDT` through the positive-EV probe, entry `0.02579`, SL `0.02667441`, TP1 `0.02446339`, TP2 `0.02357899`, size `$21.87`, risk `1.0714%`, EV net `+0.7881%`, open count `1`, equity `$70.00`, drawdown `0.0%`. Negative-EV high-score candidates continue to be rejected at `G-EV`.
- Deployed the full in-flight EV-strict/runtime bundle to VM paths and restarted `tmux` session `ninja-vm`; active process is now `./venv/bin/python -m src.main` pid `91743`. Local focused verification passed: `71 passed in 73.79s`; local `python -m compileall src tests` passed; VM config normalization prints `ok 70 0.75 5.5 5 42 8 25.0`; VM `./venv/bin/python -m compileall src tests` passed. VM lacks `pytest` in the venv, so remote pytest could not run (`No module named pytest`). Post-restart VM state: equity `$70.00`, peak `$70.00`, daily PnL `0.0%`, drawdown `0.0%`, open `0`, token `edge_recalibration_evstrict_2026_05_23`, no kill switch. Latest scan confirms negative-EV high-score candidates rejected at `G-EV`; the visible positive-EV candidate `DYM/USDT:USDT` is now truthfully rejected at `SCORE` (`score=34.0 < thresh=43.0`, `ev_net=+0.814%`) rather than misleading `paper_scope`. Lifecycle still reports `primary_edge=None`; valid alpha-context cohorts remain unproven.
- Follow-up bottleneck diagnosis: `paper_scope` was being logged before EV/score gates, which made low-score or negative-EV candidates look like sleeve bottlenecks. Moved `_paper_trade_scope_check()` after EV gates and score threshold in `src/main.py`; this does not open new risk, it only makes rejection telemetry truthful. Focused tests passed: `36 passed in 23.60s`; local and VM compile ok. Deployed `src/main.py` to VM and restarted bot in `tmux` session `ninja-vm`; active process `./venv/bin/python -m src.main` pid `86155`. VM log confirms fresh state: equity `$70.00`, drawdown `0.0%`, daily PnL `0.0%`, open `0`. First post-restart scan shows negative-EV top signals rejected at `G-EV` and opened only in shadow.
- EV-strict follow-up: generic paper EV relaxation now requires non-negative EV in `Scorer` and `_paper_ev_relax_mode`; negative EV can only pass through explicit high-conviction allowlists. Focused tests passed: `48 passed in 43.90s`.
- Paper reset now also clears restored open paper trades and risk heat on reset-token mismatch. Token bumped to `edge_recalibration_evstrict_2026_05_23` and deployed. VM log confirms `Paper reset: cleared 5 restored open paper trades`; heartbeat after reset showed equity `$70.00`, drawdown `0.0%`, daily PnL `0.0%`, open `0`.
- VM evidence after EV-strict cycle: top generic trend signals with negative EV were rejected at `G-EV` and only opened in shadow; `data/open_trades.json` had `0` trades. This preserves aggressive discovery through shadow while preventing paper risk from being spent on model-negative candidates.

- Later update: deployed bounded market-data snapshot fetch (`market_data_snapshot_concurrency=8`, `market_data_snapshot_timeout_seconds=25.0`) to VM and restarted bot; scan cycles now complete instead of hanging in OHLCV timeout storms.
- User requested removing cooldown after the fundamental change. Config now uses `paper_state_reset_token=edge_recalibration_2026_05_23`, `cooldown_after_loss_streak=0`, `cooldown_minutes=0`; daily/dd caps remain active for measured risk. VM log confirms paper reset to `$70.00`, peak `$70.00`, losses `0`, drawdown `0.0%`, daily PnL `0.0%`, and scanner restarted. After reset, VM opened paper positions again; latest observed heartbeat showed `open=5`.
- Added paper-validation stress tightening: when daily loss <= `-3.0%` or consecutive losses >= `3`, relaxed paper samples must have non-negative EV (`stress_min_ev_net_pct=0.0`). This keeps aggressive sampling from continuing into negative-EV candidates during bad-day stress.

- VM runtime is active (`./venv/bin/python -m src.main`) and paper state is fresh token `fresh_70_2026_05_21`: equity about `$66.33`, peak about `$70.77`, starting equity `$70.00`, drawdown about `6.28%`, daily PnL about `-5.25%`, no open trades, no kill switch.
- Current blocker is measured risk, not scanner outage: daily loss cap / loss streak is preventing new entries after 16 closed paper trades.
- Lifecycle `primary_edge` is still `None`; valid alpha-context cohorts are not profitable yet. Legacy `unknown|chaos` trades show net positive but must not be promoted as alpha.
- Rejection snapshot shows candidate patterns worth monitoring but not yet tradable by current policy: repeated `chaos|liquidity_sweep|long` positive-EV rejections are out-of-scope; `accumulation_compression|neutral|long` on BNB is positive-EV but remains `neutral` sleeve because compression participation/structure did not validate.
- In-flight local changes add lifecycle `primary_edge`, bootstrap reporting, and scanner priority routing toward the primary edge. The primary-edge gate was tightened so one lucky win cannot become the main edge; it now requires at least `research_min_trades` and lifecycle health gates.
- Focused verification passed: `./venv/bin/pytest -q tests/test_strategy_lifecycle.py tests/test_cohort_policy.py tests/test_bootstrap_audit.py tests/test_paper_validation.py::test_start_resets_stale_paper_state_when_reset_token_changes` -> `17 passed in 30.24s`.


## 2026-05-24 21:40 WITA — MTF price-action goal cancelled

- User cancelled the multi-timeframe price-action strategy goal and requested rollback. Local code/config/docs/tests were reverted to remove the MTF price-action detector, scorer/router/passport wiring, telemetry fields, cohort isolation, attribution PA-MTF views, and backtest PA-MTF reporting. Broader pre-existing changes outside that goal were left untouched.


## Current State

- Repo `/home/arwin/ninja_trader` is a git repo on branch `work`. The worktree is currently dirty because code updates are still in flight; do not assume local/VM sync without checking `git status`.
- Current stack is futures-only and two-way: `trend_following` is allowed long and short, `reversal` remains the main short sleeve, `compression_breakout` stays experimental, and `neutral` is residual routing only.
- `AdaptiveBrain` is active in runtime as a sizing / pair-health overlay. `MLPredictor` remains a passive interface, not the main decision brain.
- Current paper baseline seeds from `$1000`; paper-validation wideners are active, with `min_score_threshold` at `42`, `max_open_trades` base at `2`, paper override widening to `5`, `scan_pool_size=45`, and `paper_state_reset_token=sample_collection_1000_2026_05_25`.
- Scanner/sector mapping is now sector-aware and less manual: Binance Alpha has explicit config, known crypto/TradFi sectors are mapped, and unknown Binance markets can be dynamically classified from market metadata. Latest VM live check found `56` eligible pairs and `other=[]`.
- Broad market context and cross-sectional sector rotation are both active. Sector rotation is computed once per scan from fetched snapshots, persisted to passports/dataset rows, and softly adjusts score directionally; latest VM log showed `Sector rotation top: l2 rotating_in`.
- Binance Alpha has conditional score/size risk adjustment for low-quality, weak-participation, or extreme-volatility setups; clean Alpha setups are not penalized. Existing Alpha geometry and stop-distance risk guards remain active.
- Telegram heartbeat is active every minute in the current config; fund-manager reports remain the higher-level performance summary cadence every 10 closed trades.
- `CAGR` / `MAR` in the Jim report are guarded for short histories: 1-decimal display stays, but annualization is suppressed until the sample is deep enough.
- Live secrets were sanitized out of tracked markdown/config files; runtime `.env` stays on the VM.

## What Is Already Done

- Initialized git in the correct repo and linked it to GitHub.
- Removed live credential literals from markdown/config files before commit.
- Kept runtime artifacts out of the repository via `.gitignore`.
- Verified the relevant focused tests during prior PR work; rerun the smallest relevant slice before any new logic change.
- Production VM syncs should be treated as current only after an explicit deploy and verification.

## Current Runtime Behavior

- VM runtime is active in `screen` session `ninja-trader-vm`, running `./venv/bin/python -m src.main --mode paper`; latest checked pid `448557`.
- Latest checked heartbeat: `equity=$986.54`; scanner found `56` eligible pairs; sector mapping check returned `other=[]`.
- Scanner, risk guard, trade loop, market context, sector rotation, and adaptive overlays are running from the current paper configuration.
- `neutral` should not be treated as alpha in admission logic; it remains a fallback / residual bucket.
- Dual-direction trend flow is enabled in config, so short trend ideas are no longer parked by default.
- `AdaptiveBrain` can block dead pairs and scale positions, but it does not replace the router or the risk manager.

## Next Roadmap

Objective: validate that the new market-context and sector-rotation layers improve sample quality without overfitting early paper data.

Planned scope for the next session:

1. Let the VM run long enough to generate fresh post-sector-rotation trades.
   - Inspect `open_trades.json`, pending scores, and dataset rows for `sector_rotation_*` fields.
   - Confirm new setup passports persist both `market_context` and `sector_rotation`.

2. Audit sector-rotation behavior from real scan logs.
   - Check which sector is repeatedly `rotating_in` / `rotating_out`.
   - Compare top candidates before/after sector-rotation multiplier using logs and dataset rows.
   - Make sure the layer remains a soft multiplier, not an accidental hard gate.

3. Monitor Binance Alpha risk adjustment.
   - Verify risky Alpha setups show `binance_alpha_risk=...` in strategy reason.
   - Confirm clean Alpha setups are not penalized.
   - Watch whether Alpha sector still clusters losses despite the conditional penalty.

4. Keep scanner sector mapping fresh.
   - VM scanner currently has `other=[]`; if a new pair appears as `other`, first check whether Binance metadata can classify it before adding a manual mapping.

Success criteria for the follow-up session:

- Fresh trades and near-miss rows carry sector-rotation context.
- Scanner remains `other=[]` or new unknowns are explainable.
- Sector rotation changes score ordering in a modest, directionally sensible way.
- Runtime stays stable with no fresh `Traceback`/`ERROR`/`Unhandled` failures.

## Connection Metadata

- GCP VM: `34.50.16.224` (`gcp-futures`, host `short-scanner-bot`)
- GCP user: `kiosarwin`
- GCP SSH key: `/root/.ssh/gcp_futures`
- GCP admin/recovery SSH: `gcp-futures-admin` as `futures-dump-detector` with `/root/.ssh/gcp_futures`
- GitHub repo: `git@github.com:kiosarwin/ninja-trader.git`
- GitHub deploy key: `/root/.ssh/github_ninja_trader_deploy`
- Local branch: `work`
- Remote branch target: `master`
- Verified 2026-05-24: `ssh gcp-futures` logs in as `kiosarwin`; `ssh -T git@github.com` authenticates as `kiosarwin/ninja-trader`; `git ls-remote --heads origin master` succeeds.
- Note: previous VM metadata `35.231.37.107` with `/root/.ssh/gcp_35_231_37_107` currently rejects the key with `Permission denied (publickey)`.
- Do not ask again for these stable connection details unless they change.

## Important Files

- [`src/main.py`](/home/arwin/ninja_trader/src/main.py)
- [`src/risk/risk_manager.py`](/home/arwin/ninja_trader/src/risk/risk_manager.py)
- [`src/risk/kelly_sizer.py`](/home/arwin/ninja_trader/src/risk/kelly_sizer.py)
- [`src/execution/trade_manager.py`](/home/arwin/ninja_trader/src/execution/trade_manager.py)
- [`src/models/adaptive_brain.py`](/home/arwin/ninja_trader/src/models/adaptive_brain.py)
- [`src/models/cohort_policy.py`](/home/arwin/ninja_trader/src/models/cohort_policy.py)
- [`src/scoring/scorer.py`](/home/arwin/ninja_trader/src/scoring/scorer.py)
- [`config/config.yaml`](/home/arwin/ninja_trader/config/config.yaml)
- [`tests/test_risk_manager.py`](/home/arwin/ninja_trader/tests/test_risk_manager.py)
- [`tests/test_paper_validation.py`](/home/arwin/ninja_trader/tests/test_paper_validation.py)
- [`tests/test_cohort_policy.py`](/home/arwin/ninja_trader/tests/test_cohort_policy.py)

## Verified Tests

- Re-run the focused tests that touch the file you changed before handoff.
- For runtime-sensitive changes, prefer the smallest targeted pytest slice plus a config compile check.

## Safe Next Step

If resuming in a new session, do this first:

1. `ssh gcp-futures 'cd /home/kiosarwin/ninja_trader && screen -ls && pgrep -af "python -m src.main" && tail -n 160 logs/boot.log'`
2. `./venv/bin/python -c "from src.main import load_config, normalize_config; cfg=normalize_config(load_config('config/config.yaml')); print(cfg['trading']['paper_starting_equity'], cfg['risk']['min_risk_usd'], cfg['risk']['max_direction_risk_pct'], cfg['trading']['max_open_trades'], cfg['trading']['min_score_threshold'])"`
3. `ssh gcp-futures 'cd /home/kiosarwin/ninja_trader && tail -n 700 logs/boot.log | egrep -n "Sector rotation top|Scanner found|Traceback|ERROR|Unhandled" || true'`

If the live process drifts, restart `screen -S ninja-trader-vm` after confirming config. Next concrete checks: fresh trades and near-miss rows should persist `setup_passport`, `market_context`, and `sector_rotation_*`; logs should keep showing sector rotation without fresh runtime errors.

## Resume Prompt

Use this prompt in a new session:

> Continue in `/home/arwin/ninja_trader`. Read `session.md` first. The repo is active and the worktree may be dirty, so inspect `git status` before assuming anything is synced. Current paper run is seeded from `$1000`, dual-direction trend flow is enabled, `AdaptiveBrain` is active, `MLPredictor` is still a passive helper, market context and sector rotation are active, and Binance Alpha has conditional score/size risk adjustment. Verify live VM state first (`screen -ls`, `pgrep -af "python -m src.main"`, `tail -n 200 logs/boot.log`). Current key settings to preserve unless explicitly changed: `paper_starting_equity=1000`, `paper_state_reset_token=sample_collection_1000_2026_05_25`, `risk_per_trade_pct=1.5`, `min_risk_usd=0.50`, `max_open_trades=2` base with paper override to `5`, `max_direction_risk_pct=5.50`, `scan_pool_size=45`, isolated margin, and `trend_long_only=false`. Next concrete checks: scanner should remain `other=[]`, logs should show sector rotation computing, and fresh trades/near-misses should persist `sector_rotation_*` plus `market_context` metadata. Keep changes minimal and verify with relevant focused tests before handoff.

## 2026-05-24 17:31 WITA — Perluasan universe config-only

- User melihat slot masih kosong dan meminta evaluasi perluasan universe tanpa mengubah logic trading.
- Validasi VM: AGT/USDT sudah close profit via trailing_stop +1.39% (~$0.94), bukan open lagi; open_trades tinggal IN/USDT dan EIGEN/USDT.
- Penyebab kandidat belum lolos: reject terbaru didominasi SCORE, G-REG, PAPER_SCOPE, dan COHORT_POLICY; AGT/GRASS diblok cohort trend_following|trending_expansion|long yang historisnya lemah.
- Tindakan config-only: `trading.scan_pool_size` 35 -> 60 dan `filters.min_24h_volume_usdt` 100M -> 50M. Scoring/gate/entry/exit/risk logic tidak diubah.

## 2026-05-24 17:36 WITA — Koreksi scan pool supaya cycle tidak lambat

- Setelah deploy `scan_pool_size=60`, VM mulai `Fetching data for 60 pairs...` tetapi scoring belum keluar dalam window validasi awal.
- Koreksi konservatif config-only: `scan_pool_size` 60 -> 45 agar universe tetap lebih luas dari 35, tetapi lebih realistis terhadap concurrency/timeout fetch saat ini.
- `filters.min_24h_volume_usdt` tetap 50M; logic trading/scoring/gate/entry/exit tidak diubah.

## 2026-05-24 17:40 WITA — Validasi VM setelah universe 45/50M

- Runtime VM aktif pid 284529 (`./venv/bin/python -m src.main --mode paper`).
- Cycle baru memakai config final: scanner menemukan 483 eligible pairs, fetch 45 pairs, lalu scored 31 symbols.
- Belum ada entry baru pada validasi ini; reject terbaru: top B2 gagal G-REG, PHA/ARKM/AGT diblok COHORT_POLICY.
- Open trades tetap 2: IN/USDT dan EIGEN/USDT; masih ada slot kosong, tetapi gate tetap menahan kandidat yang belum layak.

## 2026-05-24 20:27 WITA — Audit kandidat belum lolos

- VM runtime aktif di `screen` session `287277.ninja-trader-vm`, pid `287281`, menjalankan `./venv/bin/python -m src.main --mode paper`.
- Config runtime tetap konservatif hasil rollback: `scan_pool_size=45`, `filters.min_24h_volume_usdt=50M`, `min_score_threshold=42`, paper override max open `5`, dan `ev_model.gate_enabled=false`.
- Risk/account state tidak sedang freeze: equity `$67.64`, starting/daily start `$70.00`, daily PnL sekitar `-3.37%`, consecutive losses `1`, kill switch kosong, open trade aktual `1` (`EIGEN/USDT` long trend_following).
- Audit 2.500 baris log terakhir menemukan rejection: `SCORE=507`, `G-REG=351`, `PAPER_SCOPE=193`, `COHORT_POLICY=51`, `G-EV=17`. Pola utama: kandidat skor tinggi non-alpha jatuh ke `PAPER_SCOPE` karena `sleeve=neutral` untuk long, sementara kandidat alpha terbaik (`PHA` long trend_following) diblok attribution cohort `trend_following|trending_expansion|long` karena histori 9 trade, WR `0.33`, PF `0.34`.
- Kesimpulan sementara: belum ada kandidat baru yang lolos karena policy/gate bekerja sesuai kontrak saat market memberi banyak neutral/chaos candidates, bukan karena scanner mati, slot penuh, atau risk kill switch. Tidak ada logic trading yang diubah dalam audit ini.

## 2026-05-24 20:39 WITA — Audit lanjutan kandidat belum lolos

- VM masih menjalankan runtime yang sama: `screen` session `287277.ninja-trader-vm`, pid `287281`, config efektif `paper_starting_equity=70`, `scan_pool_size=45`, volume floor `50M`, `min_score_threshold=42`, paper max open override `5`, `ev_model.gate_enabled=False`.
- State risk masih bukan penyebab freeze: equity `$67.64`, peak/daily start `$70.00`, consecutive losses `1`, kill switch kosong, open count `1` (`EIGEN/USDT` long trend_following). Trade ini legacy passport tanpa market context, sesuai catatan pre-context open trade.
- Audit 5.000 baris log terakhir: `2261` reject total, `SCORE=1051`, `G-REG=708`, `PAPER_SCOPE=391`, `COHORT_POLICY=91`, `G-EV=20`. Breakdown rezim: `trending_expansion|SCORE=984`, `chaos|G-REG=640`, `trending_expansion|PAPER_SCOPE=391`, `trending_expansion|COHORT_POLICY=91`, `distribution|G-REG=68`.
- Kandidat alpha terbaik tetap `PHA/USDT` long trend_following dengan score sekitar `64-69`, threshold efektif `34`, p_win `0.524`, EV sekitar `+0.01%`, tetapi diblok cohort attribution. `python -m src.report_attribution` di VM membuktikan bucket terkait: `trend_following` 9 trade, WR `33%`, PF `0.34`, net `$-2.02`; exit profile trend_following 10 trade, WR `30%`, PF `0.30`, net `$-2.36`.
- Kandidat yang melewati score tetapi tidak alpha mayoritas `sleeve=neutral not in ['trend_following']` pada long trending expansion (`NIL`, `PLUME`, `BAN`, `GUA`, `EIGEN`, `VVV`, `AGT`). Ini konsisten dengan kontrak paper scope: long hanya boleh `trend_following`; neutral tetap residual/no-trade, bukan alpha.
- Kesimpulan lanjutan: belum ada kandidat yang lolos karena kombinasi market saat ini menghasilkan banyak neutral/chaos candidates dan satu cohort trend-following long yang historinya masih buruk. Tidak ditemukan bukti slot penuh, kill switch, cooldown, scanner stall, atau EV hard gate sebagai penyebab utama. Tidak ada logic/gate yang diubah.
